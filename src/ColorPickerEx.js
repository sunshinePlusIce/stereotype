import React from 'react';
import { useState } from 'react';
import './dist/output.css';

export default ColorPicker = ({propName}) => {
    const [color, setColor] = useState('');

    return (
        <div className="flex flex-col">
            
        </div>
    ) 
}